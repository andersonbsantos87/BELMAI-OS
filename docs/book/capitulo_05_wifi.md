# Configuração Wi-Fi

O BELMAI utiliza o mecanismo de secrets do ESPHome para proteger credenciais.

Caso a conexão com a rede principal falhe, o satélite cria automaticamente um ponto de acesso temporário para recuperação.